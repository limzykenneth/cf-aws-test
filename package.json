{
  "private": true,
  "name": "cf-aws-test",
  "version": "0.0.1",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "limzykenneth <hello@limzykenneth.com>",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.267.0"
  }
}
